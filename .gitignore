# .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo

# Frontend
node_modules/
frontend/dist/
frontend/.vite/

# The built frontend, copied here by deploy/publish.ps1 so the application can
# serve it. A build output, not a source — committing it would mean two copies
# of the same page and one of them stale.
src/App/wwwroot/

# Test / coverage
[Tt]est[Rr]esult*/
coverage/
*.coverage
*.trx

# Rider / VS / VS Code
.idea/
.vs/
.vscode/*
!.vscode/extensions.json

# Node / frontend
node_modules/
dist/
frontend/.vite/

# Local config & secrets — never commit
appsettings.*.local.json
appsettings.Development.json
*.env
.env
secrets.json

# Local tooling overrides
.claude
.claude/settings.local.json

# Coding-assistant instructions — kept on disk, deliberately not published.
# The contributor-facing half of this content lives in docs/LOCAL-DEVELOPMENT.md.
CLAUDE.md
AGENTS.md
.cursorrules
docs/10-Claude-Code-Execution-Prompt.md

# OS
.DS_Store
Thumbs.db

# Docker / local data volumes
deploy/.data/

# Device-only working files — never shared (see CLAUDE.md).
local/
